impact_map: .context/directives_map.md
map_policy: index only; do not duplicate semantic rules

| ID | Status | Date | Applies_to | Rule | Forbidden | Validation | Source |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SAC-PR1 | active | 2026-08-19 | gramática da tag, parser, campos da linha | Tag DEVE caber em uma linha, junto ao símbolo, greppável. Todo campo obrigatório DEVE ter efeito de runtime ou informar o agente. | Campo obrigatório inerte. Constraint estruturada em prosa longa. Tag em múltiplas linhas. Adicionar campo sem remover custo equivalente. | Inspeção da gramática + contagem de caracteres da linha antes/depois. | Princípio 1 — compacto |
| SAC-PR2 | active | 2026-08-19 | vocabulários, warnings canônicos, validador | Vocabulário fechado e verificável por código. Tag malformada continua parseável e emite warning canônico. Regra de qualidade DEVE ter mecanismo executável. | Prosa de skill como único enforcement. Tag desaparecer do resultado por erro de sintaxe. Alvo descartado sem warning. | Fixture inválida presente no output com o warning nomeado. | Princípio 2 — normatizado |
| SAC-PR3 | active | 2026-08-19 | payload, manifesto, skills, AGENTS.md, gramática | Toda mudança DEVE preservar ou aumentar a decisão correta por caractere lido sem ferramenta. O MCP nunca entrega menos que a leitura crua da linha. | Deslocar valor para fora da linha. Exigir MCP para descobrir que SAC existe. Aumentar a linha sem dar filtro ao leitor. | Comparação alvo a alvo entre a linha crua e o payload, na mesma fixture. | Princípio 3 — agente cego |
| SAC-PL1 | active | 2026-08-19 | payload, orçamento semântico, telemetria | A unidade do orçamento DEVE ser a unidade emitida. `file` sempre relativo à raiz. Instrumento de medição reporta os bytes efetivamente escritos. | Byte de payload que não ajuda a decidir (caminho absoluto de máquina). Orçamento medido em serialização que o sistema não emite. | Medição ponta a ponta engine → CLI → adapter → stdout. | Pilar 1 — contexto gravado / economia de tokens |
| SAC-PL2 | active | 2026-08-19 | diff-check, `verify:`, `_is_covered`, registro de linguagens | O gate é declarado publicamente como **co-edit gate**. Veredicto DEVE ser independente da ordem alfabética dos caminhos. `FAIL CLOSED` preservado para extensão não registrada com tag SAC. | Prometer prova de teste ou prevenção de regressão. Falso positivo sistemático que treine o bypass. Silenciar arquivo em linguagem não registrada. Job de gate não-bloqueante. | Fixture idêntica em duas ordenações produzindo o mesmo veredicto. | Pilar 2 — prevenção de regressão |
| SAC-PL3 | active | 2026-08-19 | `_SYMBOL_REGISTRY`, marcador de comentário, matriz de compatibilidade | Linguagem é dimensão de primeira classe da compatibilidade e é publicada como tal. Escalabilidade mede-se em repositórios adotáveis. | Escalar apenas por domínio. Whitelist fechada de marcador de comentário. Matriz que omita o eixo linguagem. | Fixture positiva e negativa por linguagem registrada + dogfooding do próprio repositório. | Pilar 3 — base para escalabilidade |
| SAC-X1 | active | 2026-08-19 | `src/`, engine, CLI | Engine e CLI permanecem stdlib-only e lexicais. | AST como requisito, code graph, dependência de terceiros em `src/`, geração dinâmica de contexto pelo MCP, provenance no hot path, documentação causal paralela. | Gate de CI de imports + revisão contra a lista "Não implementar" publicada. | Lista de vetos, adotada como política pública |
| SAC-X2 | active | 2026-08-19 | tipos de tag, cenários de capillarity | Três tags e apenas três: `ARCH`, `REGR`, `DEPRECATED`. `_BASE_SCENARIOS` e `_OPTIONAL_SCENARIOS` congelados. | Criar tag nova. `DIAGNOSE` (ou qualquer cenário) promovido a base. Campo novo na linha. | Diff dos frozensets vazio ao fim de qualquer track. | Anti-inchaço de corpus |
| SAC-X3 | active | 2026-08-19 | schema, parser, payload, manifesto, gramática persistida | Todo estado antigo alcançável DEVE ter um caminho explícito: rejeitar com ação de recuperação, migrar, regenerar com confirmação, ou deletar com confirmação destrutiva. A gramática da tag é formato persistido. | Fallback silencioso. Conversão adivinhada. Crash como política de compatibilidade. Tag existente deixar de ser lida. | Matriz de estados antigos reproduzida em fixture, uma por linha, com o veredicto declarado. | Contract Break Gate |
| SAC-X4 | active | 2026-08-19 | layout do repositório, installer | Nenhum arquivo `owned` reside dentro de árvore `managed`. O installer nunca sobrescreve `owned` após criado. | Resolver `managed`/`owned` por exclusão de nome. Installer editar configuração de host MCP. | Reexecução do installer sobre manifesto modificado, conteúdo comparado byte a byte. | Regressão silenciosa verificada em produção |
| SAC-X9 | active | 2026-08-19 | projetos consumidores fora deste repositório | Nenhum consumidor existente é migrado por estes blocos. `api_robot` e `to_de_plantao` mantêm suas cópias congeladas de `sac-context/`; `rabelo-standards` é base propagadora, não consumidora, e não é tocado. | Escrever fora de `semantic-architecture-context`. Tratar `rabelo-standards` como consumidor ou como alvo de dogfooding. | Diff restrito ao repositório público. | Decisão do usuário: transferência só após testes estáveis pós-Bloco 02 |
| SAC-X5 | active | 2026-08-19 | payload CLI e MCP, `SAC_ALLOW_*` | Resposta que operou com HALT relaxado DEVE declarar quais. Paridade CLI ≡ MCP vale em bytes, para root relativo e absoluto. | Payload permissivo indistinguível de payload gated. Normalizar path em caso de corpus que compara forma do root, separador ou ordenação. | Caso de smoke por escape, ligado e desligado, e caso não-normalizado de forma de root. | Host semi-confiável |
| SAC-X6 | active | 2026-08-19 | release público, tags de versão | Uma fonte de versão. Tag `0.1.0` exige `RELEASE_GATE.md` integralmente satisfeito, item a item, com evidência citada. | Publicar defeito conhecido de pilar como limitação documentada. Literal de versão fora do SSOT. | Checklist do gate + gate de CI contra literal de versão duplicado. | Bloqueio de release acordado |
| SAC-X7 | active | 2026-08-19 | qualquer arquivo rastreado ou publicado | Zero caminho de máquina, zero nome de monorepo privado, resolução de irmão sempre relativa. | `.pyc`/`__pycache__` rastreado. Caminho absoluto em artefato de atalho de skill. Menção a `rabelo-standards` em arquivo publicado. | Gate de higiene na CI, falha dura, aplicado também sobre `git log -p`. | Impeditivo de publicação |
| SAC-X8 | active | 2026-08-19 | tracks de execução | Executor não redescobre domínio nem inventa abordagem. Toda decisão semântica, contrato, política de erro e estado antigo já está fechada no design. | Heurística. Atalho funcional. Placebo. `TODO`/stub em caminho definitivo. Simplificar caminho robusto planejado sob argumento de "funciona e está limpo". | Diff conferido contra a track; qualquer decisão semântica nova = REJECTED. | Contrato de execução deste projeto |
